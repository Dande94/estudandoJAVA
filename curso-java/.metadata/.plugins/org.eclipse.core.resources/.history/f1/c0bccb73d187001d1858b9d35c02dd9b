package classe;

public class AreaCirc {
	double raio;
	static double pi= 3.14;//com a inclusão da palavra 'static' o pi passa estar associado a classe;
	
	AreaCirc(double raioIncial){
		raio = raioIncial;
	}
	double area() {
		return pi * Math.pow(raio, 2);
	}
}
